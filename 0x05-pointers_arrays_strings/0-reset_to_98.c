#include "main.h"
#include <stdio.h>

/**
 * reset_to_98 - check the code
 * @n: intger input pointer
 *
 * Return: Always 0 (Success)
 */
void reset_to_98(int *n)
{
	int = n;

	n = 402;
	int *ptr
	ptr = 98;
	_putchar("n=%d\n", n);
	reset_to_98(&n);
	_putchar("n=%d\n", n);
	return (0);
}
